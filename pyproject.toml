[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "orthoaget"
version = "0.1.0"
dependencies = ["keyring", "selenium", "webdriver-manager"]

[tool.setuptools.packages.find]
where = ["."]
include = ["orthoaget*", "OrthoABase*"]